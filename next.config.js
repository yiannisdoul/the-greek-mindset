/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export", // Ensures static export compatibility
  // trailingSlash: true, // Helps with static exports
  // images: {
  //   unoptimized: true, // Needed if using Next.js images in static mode
  // },
};

module.exports = nextConfig;
